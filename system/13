# Name the command that find, in real time, currently running processes

top

# top - показывает таблицу, в которой выведен список запущенных процессов
# позволяет непрерывно отображать статистику процесса до тех пор, пока не будет остановлен