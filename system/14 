# Run the ‘tail -f /var/log/syslog‘ command in background

sudo tail -f /var/log/syslog &

# & - знак амперсанда позволяет запустить процесс в фоновом режиме
# tail - Отображает содержимое файла или, по умолчанию, его стандартный ввод в стандартный вывод.
# -f - Заставляет tail не останавливаться при достижении конца файла, -
# а ждать добавления дополнительных данных к входным данным.